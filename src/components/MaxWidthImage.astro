---
import { Image } from 'astro:assets';

interface Props {
    src: ImageMetadata;
    alt: string;
}

const { src, alt } = Astro.props;
---

<Image
  class="maxWidth"
  src={src}
  alt={alt}
/>

<style>
    .maxWidth {
        max-width: 100%;
        height: auto;
    }
</style>